<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <!-- SQLite can't run this app's migrations: several of them use
             raw PostgreSQL DDL (ALTER TABLE ... DROP/ADD CONSTRAINT) to
             widen CHECK constraints, which has no SQLite equivalent.
             Tests run against a real (separate) Postgres database. -->
        <env name="DB_CONNECTION" value="pgsql"/>
        <env name="DB_HOST" value="127.0.0.1"/>
        <env name="DB_PORT" value="5432"/>
        <env name="DB_DATABASE" value="fc27ac_test"/>
        <env name="DB_USERNAME" value="fc27ac_app"/>
        <env name="DB_PASSWORD" value="Frojaa270799&amp;&amp;!!"/>
        <env name="DB_URL" value=""/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
        <!-- Off by default so employee-flow tests don't have to seed a
             SessionRequest before every HTTP call. Tests that specifically
             exercise the approval flow (SessionRequestReuseTest) flip it on
             in setUp and restore in tearDown. -->
        <env name="FC27AC_SESSION_APPROVAL" value="false"/>
    </php>
</phpunit>
